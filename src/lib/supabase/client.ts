// Re-export the existing Supabase client to maintain compatibility
export { supabase } from '@/integrations/supabase/client';
export type { DBClient as SupabaseClient } from '@/types/supabase-types';